{#
/**
 * @file
 * Default theme implementation to display a paragraph.
 *
 * Available variables:
 * - paragraph: Full paragraph entity.
 *   Only method names starting with "get", "has", or "is" and a few common
 *   methods such as "id", "label", and "bundle" are available. For example:
 *   - paragraph.getCreatedTime() will return the paragraph creation timestamp.
 *   - paragraph.id(): The paragraph ID.
 *   - paragraph.bundle(): The type of the paragraph, for example, "image" or "text".
 *   - paragraph.getOwnerId(): The user ID of the paragraph author.
 *   See Drupal\paragraphs\Entity\Paragraph for a full list of public properties
 *   and methods for the paragraph object.
 * - content: All paragraph items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - paragraphs: The current template type (also known as a "theming hook").
 *   - paragraphs--type-[type]: The current paragraphs type. For example, if the paragraph is an
 *     "Image" it would result in "paragraphs--type--image". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - paragraphs--view-mode--[view_mode]: The View Mode of the paragraph; for example, a
 *     preview would result in: "paragraphs--view-mode--preview", and
 *     default: "paragraphs--view-mode--default".
 * - view_mode: View mode; for example, "preview" or "full".
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @see template_preprocess_paragraph()
 *
 * @ingroup themeable
 */
#}
{%
  set classes = [
    'paragraph',
    'paragraph--type--' ~ paragraph.bundle|clean_class,
    view_mode ? 'paragraph--view-mode--' ~ view_mode|clean_class,
    not paragraph.isPublished() ? 'paragraph--unpublished',
    'container'
  ]
%}
{% block paragraph %}
  <div{{ attributes.addClass(classes) }}>
    <div class="row">
      <div class="contact-form-info col-12 col-md-3">
        <h2 class="contact-form-heading mb-4">
          {{ site_settings.contact_form_content.contact_form_info.field_contact_form_title_heading }}
        </h2>
        <div class="contact-message mb-3">
          <h3 class="contact-form-sub-heading">Fill in the contact form</h3>
          <div class="d-flex flex-row align-items-start">
            <span class="contact-media-icon d-inline-block contact-message-icon mr-2"></span> 
            <span class="contact-media-text">{{ site_settings.contact_form_content.contact_form_info.field_contact_message }}</span>
          </div>
        </div>
        {% if site_settings.contact_form_content.contact_form_info.field_contact_phone %}
          <div class="contact-phone mb-3">
            <h3 class="contact-form-sub-heading">Talk to us</h3>
            <div class="d-flex align-items-start">
              <span class="contact-media-icon d-inline-block contact-phone-icon mr-2"></span>
              <span class="contact-media-text">{{ site_settings.contact_form_content.contact_form_info.field_contact_phone }}</span>
            </div>
          </div>
        {% endif %}
        {% if site_settings.contact_form_content.contact_form_info.field_contact_email %}
        <div class="contact-email mb-3">
          <h3 class="contact-form-sub-heading">Send us an Email</h3>
          <div class="d-flex align-items-start">
            <span class="contact-media-icon d-inline-block contact-email-icon mr-2"></span>
            <span class="contact-media-text">{{ site_settings.contact_form_content.contact_form_info.field_contact_email }}</span>
          </div>
        </div>
        {% endif %}
        <div class="contact-social-media mb-3">
          <h3 class="contact-form-sub-heading">Connect with Us</h3>
          {% if site_settings.contact_form_content.contact_form_info.field_twitter_page_url %}
            <div class="d-flex align-items-start contact-social-media-tw mb-2">
              <span class="contact-media-icon d-inline-block contact-social-media-tw-icon mr-2"></span>
              <a href="{{ site_settings.contact_form_content.contact_form_info.field_twitter_page_url.uri }}">
                {{ site_settings.contact_form_content.contact_form_info.field_twitter_page_url.title }}
              </a>
            </div>
          {% endif %}
          {% if site_settings.contact_form_content.contact_form_info.field_facebook_page_url %}
            <div class="d-flex align-items-start contact-social-media-fb mb-2">
              <span class="contact-media-icon d-inline-block contact-social-media-fb-icon mr-2"></span>
              <a href="{{ site_settings.contact_form_content.contact_form_info.field_facebook_page_url.uri }}">
                {{ site_settings.contact_form_content.contact_form_info.field_facebook_page_url.title }}
              </a>
            </div>
          {% endif %}
          {% if site_settings.contact_form_content.contact_form_info.field_instagram_page_url %}
            <div class="d-flex align-items-start contact-social-media-ig mb-2">
              <span class="contact-media-icon d-inline-block contact-social-media-ig-icon mr-2"></span>
              <a href="{{ site_settings.contact_form_content.contact_form_info.field_instagram_page_url.uri }}">
                {{ site_settings.contact_form_content.contact_form_info.field_instagram_page_url.title }}
              </a>
            </div>
          {% endif %}          
        </div>    
      </div>
      <div class="contact-form-form-wrapper col-12 col-md-5 offset-md-1">
        <div class="contact-form-required-fields py-4">{{ "Fields marked with an asterisk * are required" }}</div>
        {% block content %}
          {{ content }}
        {% endblock %}
      </div>
    </div>
  </div>
{% endblock paragraph %}
